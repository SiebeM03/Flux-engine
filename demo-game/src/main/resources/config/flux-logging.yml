defaultLevel: DEBUG

categories:
  timer: INFO
  opengl: INFO

startupBannerConfig:
  enabled: true
  log-system-info: true

outputs:
  - type: console
    pattern: "[{level}] [{category}] {message}"
  - type: file
    file: "logs/flux.log"
    pattern: "[{time}] [{level}] [{category}] {message}"